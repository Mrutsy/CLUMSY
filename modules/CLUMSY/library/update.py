import os
import subprocess


def check_github():
    # useless_cat_call = subprocess.run(["git status"], stdout=subprocess.PIPE, text=True, input="Hello from the other side")
    # print(useless_cat_call.stdout)  # Hello from the other side
    pass
    # list_dir = subprocess.Popen(["git status"])
    # list_dir.wait()

    # list_files = subprocess.run(["git status", "-l"])
    # print(list_files)
    # status = os.system("git status")
    # print(status[:4])
